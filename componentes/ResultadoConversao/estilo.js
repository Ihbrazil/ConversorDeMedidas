import { StyleSheet } from "react-native";

const estiloInput = StyleSheet.create({
  textoContainer: {
    marginTop: 35,
    paddingVertical: 60,
  },

  label: {
      fontSize: 120,
      
      color: '#fff',
      textAlign: "center",
  },

});

export default estiloInput;